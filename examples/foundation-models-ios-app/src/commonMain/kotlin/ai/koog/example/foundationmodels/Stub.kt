package ai.koog.example.foundationmodels

/**
 * Placeholder so the non-iOS targets (JVM/JS/Android/wasmJs) registered by the convention
 * plugin have a non-empty commonMain. The real entry point ([runFoundationModelsSmokeTest])
 * lives in appleMain because Foundation Models is iOS-only.
 */
@Suppress("unused")
private class Stub
